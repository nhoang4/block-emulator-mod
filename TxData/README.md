# Transaction Data

Large transaction CSVs are intentionally not stored in Git.

For the saved default experiment config, copy this file into this directory on
each machine:

```text
1000000to1999999_BlockTransaction.csv
```

The default config expects it at:

```text
./TxData/1000000to1999999_BlockTransaction.csv
```
